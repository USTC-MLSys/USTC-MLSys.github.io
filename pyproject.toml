[project]
name = "open-horizon-lab-site"
version = "0.1.0"
description = "A Python-powered static website for a research lab."
requires-python = ">=3.11"

dependencies = []

[tool.setuptools.packages.find]
include = ["labsite*"]

[tool.website]
source = "content"
output = "dist"
